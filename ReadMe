AS连接github进行远程提交
1. 终端下载git工具并配置在AS上：AS——》preference ——》 git
2. 配置本地仓库 git——》 cerate directory
3. git add
4. git commit
5. push 到 github
 5.1 检查配置 git config --list  检查name和email是否配置完成
 5.2 git push 操作
